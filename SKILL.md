---
name: mail-workbench
display_name: 邮件工作台（本地邮箱工作视图）
description: 在本地安装并使用「邮件工作台」—— 把你的邮箱整理成「待我处理 / 等对方回复 / AI 草稿」这样的工作视图，而不是一个堆满未读的收件箱。全部数据留在本机，不上传任何服务器。当用户说「安装邮件工作台 / 装一下邮件工作台 / 打开工作台 / 启动工作台 / 处理邮件草稿队列 / 邮件工作台怎么用」时使用本技能。本技能负责引导安装、启动服务、解释用法；**不负责**「看邮件 / 邮件日报 / 帮我回信」这类直接读写邮箱的请求（那是邮箱类技能的事）。
version: 1.0.0
agent_created: true
---

# 邮件工作台 · 安装与使用引导

## 这是什么

一个**跑在你自己电脑上**的邮件整理工具。它把邮箱变成工作视图：

| 视图 | 含义 |
|---|---|
| **待我处理** | 球在你这边，需要你看或回 |
| **等待对方回复** | 你发出去了，等对方回音（超期自动沉底，不占计数） |
| **AI 草稿** | 需要你审核的草稿 |
| **已延后 / 今天完成** | 你自己安排的节奏 |

它**不做**的事：不替你发邮件（发送必须你亲手点两次确认）、不上传你的邮件到任何服务器。

## 你该走哪条路

| 用户说 | 做什么 |
|---|---|
| 「安装邮件工作台」「装一下」「怎么装」 | 走 §1 安装流程 |
| 「打开工作台」「启动工作台」「打不开」 | 走 §2 启动与排障 |
| 「工作台怎么用」「有哪些功能」 | 走 §3 用法说明 |
| 「处理邮件草稿队列」 | 走 §4 草稿队列（Worker 流程） |
| 「看邮件」「邮件日报」「帮我回信」 | **不是本技能**，提示用户用邮箱类技能 |

## §1 安装

**前提**：WorkBuddy 已安装（本工具会复用它的内置 Python，无需另装）。

### 步骤

1. **找到代码目录** —— 通常就是本技能所在目录：
   `~/.workbuddy/skills/mail-workbench/`
   （若用户是从 GitHub 单独 clone 的，以实际路径为准。）

2. **确认目录里有 `install.cmd`**。没有的话说明代码没下全，让用户重新导入技能。

3. **让用户自己运行安装向导**（**不要替用户跑** —— 向导要输入邮箱和密码，
   密码不该经由你传递）：

   让用户打开该目录，**双击 `install.cmd`**。

4. 向导会依次做：
   - 自动探测本机 Foxmail 的索引目录，并尝试从路径猜出邮箱地址
   - 询问邮箱地址与密码（IMAP/SMTP 授权码）
   - **当场验证能否登录**
   - 写 `config.json` + 凭据文件（凭据存在 `~/.workbuddy/secrets/`，不进项目目录）
   - 可选：创建桌面图标 / 开机自启

5. 装完后让用户双击 `启动V2工作台.cmd`，浏览器会打开
   `http://127.0.0.1:8080/workbench`。

### 安装时的常见拦截

| 现象 | 原因 | 处理 |
|---|---|---|
| 提示找不到 Python | 没装 WorkBuddy，或它的内置 Python 不在此路径 | 装 WorkBuddy，或自行安装 Python 3.8+ |
| 登录验证失败 | 用的是登录密码而非 **IMAP 授权码** | 到学校邮箱设置里开启 IMAP 并生成授权码 |
| 探测不到 Foxmail 索引 | 没装 Foxmail，或索引路径非默认 | 在向导里手动填索引目录；或跳过（只用 IMAP 同步） |
| 端口 8080 被占用 | 服务已经在跑了 | 直接打开页面即可，无需重复启动 |

## §2 启动与排障

**正常启动**：双击 `启动V2工作台.cmd`（幂等：已在跑就直接开浏览器）。

**打不开时**，按顺序看三个日志（都在代码目录下）：

1. `_launch.log` —— 启动器自己的记录（含异常堆栈）
2. `_watchdog.log` —— 守护进程有没有在拉服务
3. `_server.log` —— 服务自身的输出与崩溃堆栈

**三个日志都没新内容** → 多半是 Python 解释器不对，让用户重跑 `install.cmd`。

> 常见误解：本机若设了系统代理，访问 `127.0.0.1` 会被代理接走并返回 502，
> 看起来像"服务挂了"。这不是服务的问题。检查 `no_proxy` 是否包含 `127.0.0.1`。

## §3 用法说明（讲给用户听）

- **导入邮件**：首次启动会同步 IMAP；如果本机装了 Foxmail，
  还能把它的历史邮件索引进来（毫秒级，不需要重新下载）。
- **日常节奏**：只看「待我处理」这一栏。其他桶是信息，不是待办。
- **回复一封邮件**：
  - 「⚡ 快速草稿」——本地模板**立刻**出稿（收到确认 / 婉拒 / 索取材料 /
    确认时间 / 致谢 / 稍后答复），带 `[请补充：…]` 的地方需要你自己填。
    事务性回复用这个，不必等 AI。
  - 「✎ AI 起草」——排进队列，等 WorkBuddy 认领后生成更贴合的措辞。
- **发送**：必须两步 —— 先点「确认发送」拿一次性令牌，再点「真正发送」。
  这不是麻烦，是防止误发和误操作的设计。
- **分类标签**：默认按高校场景配好了（科研管理、基金委、投稿系统等）。
  不满意就在设置里改关键词，改完刷新页面生效。

## §4 草稿队列（Worker 流程）

工作台的「AI 起草」只是把任务**排进队列**，需要一个 Worker 来认领才会生成草稿。

代码目录自带命令行工具（16 个子命令）。**先诊断**：

```
python -m mail_workbench.cli doctor                 # 健康检查
python -m mail_workbench.cli buckets                # 看工作桶
python -m mail_workbench.cli jobs --status queued   # 看排队中的任务
python -m mail_workbench.cli show <job_id> --context
```

想看契约闭环跑起来是什么样（不含 AI，只演示流程）：

```
python -m mail_workbench.cli worker --rounds 1
```

### 你要亲自写草稿时，走 HTTP

1. **认领**：`GET /api/v2/draft-jobs/next?worker=<名字>`
2. **取上下文**：`GET /api/v2/draft-jobs/{id}/context?worker=<名字>`
3. **写回**：`POST /api/v2/draft-jobs/{id}/draft`（body 带 `draft_text`、`worker`）

**两条硬边界**：

1. **绝不发送**。契约里没有 send 端点。发送只接受 `approved` 状态
   + 一次性令牌 + `confirm="SEND"`，且必须由人在界面上点。
2. **绝不编造**。若涉及「是否同意 / 有没有名额 / 具体日期金额 / 要发哪个附件」
   而上下文里没有依据，**必须**提交 plan 并把状态置 `NEEDS_INPUT`，
   让用户自己填，而不是替他猜。

> 租约 300 秒。写不完要续租（`POST /api/v2/draft-jobs/{id}/heartbeat`），
> 否则会被回收重投。

## §5 隐私边界（用户问起时的回答）

- **所有数据在本机**：邮件正文、附件、草稿都存在你电脑上的 SQLite 与文件里。
  本工具不上传任何内容到外部服务器。
- **密码不进项目目录**：凭据写在 `~/.workbuddy/secrets/`，权限只有你自己可读。
- **AI 只在你明确要求时才读邮件**：草稿生成是你点了按钮才发生的。
- **不会自动发送任何邮件**：代码里只有 `approved` 状态才允许出站。
- **卸载**：删掉代码目录即可；凭据文件在 `~/.workbuddy/secrets/`，可单独删除。
