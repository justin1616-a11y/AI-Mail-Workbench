# 安装指南

面向使用者。想了解实现细节请看 `docs/`。

---

## 一、前置条件

| 项目 | 要求 |
|---|---|
| 操作系统 | **Windows 10 / 11**（当前版本仅支持 Windows） |
| WorkBuddy | 需已安装（复用其内置 Python，**不需要你另外装 Python**） |
| 邮箱 | 支持 IMAP 与 SMTP 的邮箱（交大邮箱需先开启 IMAP 并生成授权码） |
| 磁盘 | 约 50 MB（不含历史邮件索引） |

> **不需要**：Python 环境、Node.js、任何 `pip install`。代码只用 Python 标准库。

---

## 二、安装

### 方式一：通过 WorkBuddy 技能导入（推荐）

1. 打开 WorkBuddy → 「技能」面板 → 「添加技能」
2. 选择 **「从 GitHub 导入」**，粘贴本仓库地址
3. 安装完成后，在对话框说：**「安装邮件工作台」**
4. 按引导完成配置

技能会把代码放在：
```
C:\Users\<你的用户名>\.workbuddy\skills\mail-workbench\
```

### 方式二：手动下载

1. 下载本仓库（`Code → Download ZIP`）
2. 解压到**不含中文和空格的路径**（例如 `D:\mail-workbench`）
3. 双击 **`install.cmd`**

> 为什么建议不要放在中文路径下：部分 Windows 命令行组件对非 ASCII 路径处理不一致，
> 可能造成启动脚本行为异常。

---

## 三、配置向导

双击 `install.cmd` 后，向导会依次进行：

### 1. 自动探测

扫描本机 Foxmail 的索引目录。**如果本机装了 Foxmail**，向导会：
- 找到索引文件
- 尝试从路径中解析出你的邮箱地址

探测不到也没关系，下一步手动填。

### 2. 填写邮箱与凭据

| 字段 | 说明 |
|---|---|
| 邮箱地址 | 完整地址，如 `yourname@sjtu.edu.cn` |
| 密码 | ⚠️ **是 IMAP/SMTP 授权码，不是登录密码** |

**关于授权码**：

多数高校邮箱为安全考虑，要求第三方客户端使用独立的授权码。
获取方式通常是：登录学校邮箱网页版 → 设置 → 「IMAP/SMTP 服务」→
开启服务 → 生成授权码。

> 如果你的学校邮箱**不提供 IMAP**，本工具无法使用 —— 这是服务端限制，不是工具的缺陷。
> 可以先用 Foxmail / Outlook 客户端试一下能不能添加你的邮箱，能加就说明支持 IMAP。

### 3. 当场验证

向导会**立刻测试 IMAP 与 SMTP 连接**。失败会明确告诉你卡在哪一步：

| 报错 | 多半是 |
|---|---|
| 认证失败 | 用了登录密码而不是授权码 |
| 连接超时 | 主机/端口填错，或本机网络/代理拦截 |
| 服务不支持 | 邮箱未开启 IMAP |
| 证书错误 | 公司的网络中间人证书；换网络环境试试 |

### 4. 写入配置

- `config.json` —— 写在**代码目录**下（不含密码）
- 凭据文件 —— 写在 `C:\Users\<你>\.workbuddy\secrets\mail.cred`（**不在项目目录**）

### 5. 可选项

- 创建桌面图标（双击 = 启动服务 + 打开浏览器）
- 设置开机自启

---

## 四、启动

双击 **`启动V2工作台.cmd`**。

它会：

1. 检查服务是否已在运行（幂等，不会重复启动）
2. 拉起来（无窗口后台运行）
3. 等服务真的能响应了，再打开浏览器

浏览器打开的是 `http://127.0.0.1:8080/workbench`。

> 首次启动会连接 IMAP 同步，可能慢一些（取决于邮件量）。
> 之后启动是秒开。

---

## 五、排障

### 页面打不开

按顺序检查代码目录下的三个日志：

| 文件 | 看什么 |
|---|---|
| `_launch.log` | 启动器自己的记录，含异常堆栈 |
| `_watchdog.log` | 守护进程有没有在拉服务 |
| `_server.log` | 服务自身的输出与崩溃堆栈 |

**三个都没新内容** → 多半是 Python 解释器不对，重跑 `install.cmd`。

### 页面显示 502

**本机设了系统代理，把 `127.0.0.1` 的请求也接走了。**

把 `127.0.0.1` 加入代理例外（`no_proxy`），或临时关闭系统代理。

> 这一条能骗过很多人：服务明明在跑，端口也通，但浏览器就是报错。
> 用 `curl --noproxy '*' http://127.0.0.1:8080/api/health` 可以验证是不是代理的问题。

### 端口 8080 被占用

可能已经有服务在跑了 —— 直接打开页面即可。

要换端口的话，改 `config.json` 里的 `port`，然后重启。

### 启动后立刻闪退

如果是双击 `.cmd` 时窗口一闪而过：脚本结尾有 `pause`，
理论上会停住。如果确实闪退，说明是 `pythonw.exe` 静默失败 —— 看 `_launch.log`。

（`pythonw.exe` 没有控制台，异常不会显示，只会写日志。）

---

## 六、配置说明

`config.json` 写在代码目录下（`config.example.json` 是模板）。

| 字段 | 含义 | 默认 |
|---|---|---|
| `user` | 邮箱地址 | 必填 |
| `imap_host` / `imap_port` | IMAP 服务器 | 必填 |
| `smtp_host` / `smtp_port` | SMTP 服务器 | 必填 |
| `from_name` | 发信时显示的名字 | 建议填 |
| `sync_folders` | 要同步的文件夹列表 | `INBOX` / `Sent` 等 |
| `sent_folder` | 已发送文件夹名 | `Sent` |
| `action_window_days` | 「待我处理」只看最近多少天 | 30 |
| `waiting_window_days` | 「等待对方回复」的活跃窗口 | 14 |
| `port` | 本地服务端口 | 8080 |

改完 `config.json` 需要**重启服务**才生效。

> 修改分类关键词不需要改代码：在界面里改，刷新页面即可。

---

## 七、卸载

1. 停止服务：任务管理器结束 `pythonw.exe`（或删除代码目录后重启）
2. 删除代码目录
3. 删除凭据：`C:\Users\<你>\.workbuddy\secrets\mail.cred`
4. 如果设了开机自启：在「任务计划程序」里删除对应任务

**删掉后没有任何残留**（除了你自己的邮箱服务器上原有的邮件 —— 本工具不改动服务器上的邮件内容）。

---

## 八、给开发者

### 直接跑

```
python -m mail_workbench.server            # 正式启动（含 IMAP IDLE + scheduler）
python -m mail_workbench.server --no-idle  # 只开界面（离线调试）
```

### 跑测试

```
python -m unittest mail_workbench.tests.test_v2 -v
python -m unittest mail_workbench.tests.test_bridge -v
```

测试全部使用临时数据库与打桩的网络层，**不会连接真实邮箱**。

### 目录结构

```
mail_workbench/
├── mail/           IMAP / SMTP / 邮件解析 / 附件 / Foxmail 索引读取
├── storage/        SQLite 仓储层、检索
├── thread/         线程聚合、上下文构建
├── workflow/       工作桶计算、状态机、动作、跟进、延后
├── draft/          草稿队列、Worker 契约、模板、恢复
├── intelligence/   分类规则、事实抽取、回复规划
└── ui/             工作台前端（单文件 HTML）
```

### 两条核心约束

改动代码时请遵守：

1. **AI 永不发送邮件**。发送只允许从 `approved` 状态出发，
   且需要一次性令牌 + 显式 `confirm="SEND"`，全部由人触发。
2. **本地优先**。分类、去重、线程、队列、工作桶计算全部本地完成；
   只有需要"读懂并组织信息"的起草任务才交给 AI。
